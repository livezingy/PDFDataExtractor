# Streamlit Cloud部署依赖文件
# 用于Streamlit Cloud一键部署
# 注意：Transformer功能仅在本地环境可用

# Streamlit核心
streamlit>=1.28.0

# PDF处理
pdfplumber>=0.9.0
PyMuPDF>=1.23.0
camelot-py>=0.11.0

# PaddleOCR（推荐用于图像处理）
# 注意：PaddleOCR 3.x 需要 PaddlePaddle 3.0+ 才能正常工作
# PPStructureV3 需要 paddleocr[doc-parser] 依赖，paddleocr[all] 已包含所有功能
# PP-StructureV3 还需要 paddlex[ocr] 额外依赖才能正常工作
# 版本要求：PaddleOCR 3.0+ 与 PaddlePaddle 3.0+ 必须匹配
# PaddleOCR 3.3.2 需要 PaddleX 3.3.x 版本（3.3.0 <= version < 3.4.0）
# 注意：paddlex[ocr] 会自动安装基础 paddlex 包及其 OCR 扩展依赖
# Streamlit Cloud 使用 Python 3.13，必须使用 PaddleOCR 3.x + PaddlePaddle 3.x
# PaddleOCR 2.x 不支持 Python 3.13（PaddlePaddle 2.x 只支持到 Python 3.11）
paddleocr[all]>=3.0.0
paddlepaddle>=3.0.0
paddlex[ocr]>=3.3.0

# 数据处理
pandas>=2.0.0
numpy>=1.23.0
scipy>=1.10.0
matplotlib>=3.7.0

# 图像处理
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# 工具库
scikit-learn>=1.3.0
tqdm>=4.65.0
python-dateutil>=2.8.2
sympy>=1.12.0

# 注意：以下依赖仅在本地环境需要（Transformer功能）
# torch>=2.0.0
# torchvision>=0.15.0
# transformers>=4.30.0
# timm>=0.6.0
# huggingface-hub>=0.16.0
# easyocr>=1.7.0

