# Streamlit专用依赖文件
# 用于Streamlit Cloud部署

# Streamlit核心
streamlit>=1.28.0

# PDF处理（基础，不包含Camelot的系统依赖）
pdfplumber>=0.9.0
PyMuPDF>=1.23.0

# 数据处理
pandas>=2.0.0
numpy>=1.23.0
scipy>=1.10.0

# 图像处理
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# 可选：Camelot（如果Streamlit Cloud环境支持）
# 注意：Camelot需要ghostscript和tesseract系统依赖
# 在Streamlit Cloud中可能不可用，需要降级处理
camelot-py>=0.11.0

# 可选：深度学习模型（如果需要Transformer功能）
# 注意：模型文件较大，可能需要从外部存储加载
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0

# 可选：OCR（如果需要）
easyocr>=1.7.0
# pytesseract>=0.3.10

# 工具库
scikit-learn>=1.3.0
tqdm>=4.65.0

